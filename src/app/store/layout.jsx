
export const metadata = {
    title: 'Quity',
    description: 'Miner site',
}

export default function StoreLayout({ children }) {
    return (
        <div>
            Store
            {children}
        </div>
    )
}
